# Path to the SQLite database file.
# Defaults to local.db in the project root if not set.
DATABASE_URL=local.db

# Bearer token required to call POST /api/sync (manual roster refresh).
# The endpoint returns 503 until this is set.
SYNC_TOKEN=
